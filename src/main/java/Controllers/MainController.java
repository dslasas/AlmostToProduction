package Controllers;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

@SpringBootApplication
public class MainController {
    /* Todo : Complete 'Building REST services with Spring' and use it to create some simple REST operations */
    /* Todo : How to map objects to database tables */
    /* Todo : Connecting Spring application to a database */
    /* Todo : Figure out how to deploy to AWS */
    public static void main(String[] args) {
        SpringApplication.run(MainController.class, args);
    }
}
